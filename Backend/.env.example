DB_URL="your-mongodb-connection-string-here"
JWT_SECRET_KEY="replace-with-a-secure-random-string"

# Optional: admin credentials for offline login / initial seeding
# It's recommended you change the password before using in production.
ADMIN_EMAIL=fahadhedayet0@gmail.com
ADMIN_PASSWORD=F@H@D0123
